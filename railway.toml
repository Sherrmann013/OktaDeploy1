[build]
builder = "nixpacks"

[deploy]
startCommand = "echo 'Starting app directly...' && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "always"

[variables]
NODE_ENV = "production"
PORT = "3000"