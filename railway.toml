[build]
builder = "nixpacks"

[deploy]
startCommand = "echo 'Starting app...' && echo 'PORT: '$PORT && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 600
restartPolicyType = "always"

[variables]
NODE_ENV = "production"
PORT = "3000"